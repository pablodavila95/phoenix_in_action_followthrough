<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AuctionWeb · Phoenix Framework</title>
  <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
  <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>

<body>
  <header>
    <section class="container" style="border-bottom: 1px solid #77777; margin-bottom: 2rem; padding-bottom: 1rem;">
      <%= link "Items", to: Routes.item_path(@conn, :index) %>
      <%= if @current_user do %>
        Logged in as <%= link @current_user.username, to: Routes.user_path(@conn, :show, @current_user) %>
        <%= link "Log out", to: Routes.session_path(@conn, :delete), method: :delete %>
      <% else %>
        <%= link "Login", to: Routes.session_path(@conn, :new) %>
        <%= link "LiveDashboard", to: Routes.live_dashboard_path(@conn, :home) %>
      <% end %>
    </section>
  </header>
  <div class="container" style="border-bottom: 1px solid #77777; margin-bottom: 2rem; padding-bottom: 1rem;">

  </div>
  <main role="main" class="container">
    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    <%= @inner_content %>
  </main>
  <div class="container" </body> </html>
